package src;

abstract class DatabaseModel {
    /* TODO: need to implement:
     * [ ] id stuff
     * [ ] CRUD methods (save, delete)
     * [ ] static methods for all, first, where
     * [ ] getColumns() to specify columns (excluding id)
     * [ ] toString() for display
     */
}